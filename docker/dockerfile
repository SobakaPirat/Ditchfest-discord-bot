FROM python:3.13-slim

# Устанавливаем uv
RUN pip install --no-cache-dir uv

WORKDIR /app

# Копируем файлы проекта
COPY pyproject.toml uv.lock ./
COPY updater.py notifier.py ./
COPY src/ ./src/

# Устанавливаем зависимости через uv
RUN uv sync --frozen --no-dev